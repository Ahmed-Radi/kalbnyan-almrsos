let info = {
    name: "Ahmed",
    age: 22,
    location: 'Helwan Cairo, Egypt'
}

console.log(`${info.name} is ${info.age} lives in ${info.location}`)
info.age += 1;
console.log(`${info.name} is ${info.age} lives in ${info.location}`)